<UserControl x:Class="Albion.GUI.Controls.RequirementControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Albion.GUI"
             mc:Ignorable="d" 
             Name="Root"
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel>
        <DockPanel.Resources>
            <Style TargetType="TextBlock">
                <Setter Property="Margin" Value="2"/>
            </Style>
        </DockPanel.Resources>
        <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" Margin="2"/>
        <!-- <TextBlock Name="TitleBox" Text="{Binding Title, ElementName=Root}" /> -->
        <TextBlock Name="TitleBox" Text="{Binding Title, ElementName=Root}" />
        <TextBlock Text="{Binding Cost, StringFormat=Cost: {0}, Converter={StaticResource PriceConverter}}" />
        <TextBox Text="{Binding Silver, Mode=TwoWay, Converter={StaticResource PriceConverter}}" />
    </DockPanel>
</UserControl>
